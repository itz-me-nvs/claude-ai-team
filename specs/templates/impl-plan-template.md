# FEAT-XX-XX — Implementation Plan

> Companion to `specs/features/FEAT-XX-XX-<name>.md`. Required for M/L effort or anything touching >1 module/file. Skip for trivial S-effort changes (single file, cosmetic, no new logic) — if skipped, don't create this file; note "Skipped: trivial" + why in the feature spec §8 instead.
> Not full code. Enough for a dev to pick this up cold.

## Metadata
| Field | Value |
|-------|-------|
| Feature spec | `specs/features/FEAT-XX-XX-<name>.md` |
| Status | `draft` \| `approved` |

---

## Files Touched
- `path/to/file.ts` — new / modified / deleted, what changes, why

---

## Approach
Step-by-step build order (1, 2, 3…). Call out sequencing (schema → API → UI), shared code to reuse, new deps.

---

## How to Use / Verify
Commands, routes/screens to hit, tests to run once built.

---

**Status:** `draft` \| `approved` — builders do not start until `approved`.
